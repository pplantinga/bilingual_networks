# Recipe for quantizing audio into discrete tokens
# Author: Peter Plantinga
seed: 3421
__set_seed: !apply:speechbrain.utils.seed_everything [!ref <seed>]
output_folder: !ref results/quantize_whisper_en/<seed>
output_wer_folder: !ref <output_folder>/
save_folder: !ref <output_folder>/save
train_log: !ref <output_folder>/train_log.txt

# Data files
pretrained_path: !PLACEHOLDER
data_folder: data
train_fr_manifest: manifests/train_fr.json
valid_fr_manifest: manifests/valid_fr.json
test_fr_manifest: manifests/test_fr.json
train_en_manifest: manifests/train_en.json
valid_en_manifest: manifests/valid_en.json
test_en_manifest: manifests/test_en.json
train_de_manifest: manifests/train_de.json
valid_de_manifest: manifests/valid_de.json
test_de_manifest: manifests/test_de.json
train_language: "en"
train_languages: [!ref <train_language>] # For the dataloader, need a list too
test_portion: 0.02

#ckpt_interval_minutes: 15 # save checkpoint every N min

####################### Training Parameters ####################################
number_of_epochs: 20
num_workers: 4
batch_size: 64

# Feature parameters
sample_rate: 16000
n_fft: 400
n_mels: 80

train_dataloader_opts:
    num_workers: !ref <num_workers>
    batch_size: !ref <batch_size>

valid_dataloader_opts:
    num_workers: !ref <num_workers>
    batch_size: !ref <batch_size>

test_dataloader_opts:
    num_workers: !ref <num_workers>
    batch_size: !ref <batch_size>

epoch_counter: !new:speechbrain.utils.epoch_loop.EpochCounter
    limit: !ref <number_of_epochs>

train_logger: !new:speechbrain.utils.train_logger.FileTrainLogger
    save_file: !ref <output_folder>/train_log.txt
    precision: 3

compute_features: !new:speechbrain.integrations.huggingface.whisper.Whisper
    source: !ref <pretrained_path>
    save_path: !ref <output_folder>
    encoder_only: True
    freeze: True
    output_all_hiddens: True
    language: !ref <train_language>


modules:
    compute_features: !ref <compute_features>

# Avg pool whisper features to get 4x time reduction
pool1d: !new:speechbrain.nnet.pooling.Pooling1d
    pool_type: avg
    kernel_size: 2
    pool_axis: 1  # time
    stride: 2

# Quantizer parameters
n_clusters: 2000
#init: k-means++
init: random
kmeans_batch_size: 10000  # Should be >= num_clusters

quantizer: !new:speechbrain.integrations.audio_tokenizers.kmeans.MiniBatchKMeansSklearn
    n_clusters: !ref <n_clusters>
    init: !ref <init>
    max_iter: 100
    batch_size: !ref <kmeans_batch_size>
    tol: 0.0
    max_no_improvement: 100
    n_init: 20
    reassignment_ratio: 0.01
    random_state: !ref <seed>
    verbose: 1
    compute_labels: True
    init_size: null

checkpointer: !new:speechbrain.utils.checkpoints.Checkpointer
    checkpoints_dir: !ref <save_folder>
    recoverables:
        counter: !ref <epoch_counter>
        quantizer: !ref <quantizer>
